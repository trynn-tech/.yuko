`home-manager switch --flake .#yuko-core`
`mbsync -V trynn-primary` - Update email manually 
.yuko/.yuko-env.nix is the directory where the user's home or anchor path is located


TODO: 
- Clean-Up ysnow ... perhaps shorten example
- Refactor to make lean yuko-dev env for meta testing (currently core.nix is inflexible)

Praxis:
tw
tig

incorporate ```eval $(ssh-agent -s)
ssh-add ~/.ssh/id
```
